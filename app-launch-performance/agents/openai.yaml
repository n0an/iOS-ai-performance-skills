interface:
  display_name: "App Launch Performance"
  short_description: "Cold/warm/hot launch, pre-main work, dyld, +load, MetricKit."
  brand_color: "#F05138"
  default_prompt: "Use $app-launch-performance to review this iOS code for performance."

policy:
  allow_implicit_invocation: true
