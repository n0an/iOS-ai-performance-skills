interface:
  display_name: "SwiftUI Performance"
  short_description: "View identity, body recomputation, @Observable, lazy containers."
  brand_color: "#F05138"
  default_prompt: "Use $swiftui-performance to review this iOS code for performance."

policy:
  allow_implicit_invocation: true
